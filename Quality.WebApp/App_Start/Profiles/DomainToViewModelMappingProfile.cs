using AutoMapper;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Quality.WebApp
{
    public class DomainToViewModelMappingProfile : Profile
    { 
    //{
    //    cfg.CreateMap<QueryResult<UserSummary>, UserQueryResultResourceData>()
    //          .ForMember(x => x.Items, opts => opts.MapFrom(x => x.Items));

    //    Mapper.CreateMap<DataAccess.ref_Machine, Domain.Machine>()
    //            .ForMember(o=>o., opt=>opt.MapFrom(o=>o.LastName));
    }
}